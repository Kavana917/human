/**
 * @deprecated Use AbductionTest or AdductionTest. Kept as a thin alias for
 * generic /test/:testId routes (flexion, etc.) until those get dedicated pages.
 */
import MovementTestRecorder from './MovementTestRecorder';

export default function AbductionAdduction() {
  return <MovementTestRecorder />;
}
